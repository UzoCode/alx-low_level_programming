#include "main.h"

/**
 * main - check the code
 * @n: character to check
 * Return: 1 if n greater than zero and 0 or -1 for other character
 *
 */
int print_sign(int n)
{
	n = 110

	if (n > 0 && )
}
